# Blackjack | React - NodeJS 

To run, simply run the following commands in the root directory of `client` and `server`:

```
yarn
yarn dev
```

You can change countdown and name in `App.tsx`.

Scores are saved using `json-server` in `db.json` file.